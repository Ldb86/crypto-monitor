server:
  port: ${PORT:3000}

maradona:
  symbols: ${SYMBOLS:ETHUSDT,BTCUSDT}
  bybit:
    wsUrl: ${BYBIT_WS_URL:wss://stream.bybit.com/v5/public/linear}
  telegram:
    botToken: ${TELEGRAM_BOT_TOKEN:${BOT_TOKENS:}}
    chatId: ${TELEGRAM_CHAT_ID:${CHAT_IDS:}}
  tv:
    secret: ${MARADONA_SECRET:maradona1838}
  risk:
    maxScoreToNotify: 6
